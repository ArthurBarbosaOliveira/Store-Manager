const NotFoundError = 'Product not found';

const nameRequired = '"name" is required';

const nameLength = '"name" length must be at least 5 characters long';

module.exports = {
  NotFoundError, nameRequired, nameLength,
};
